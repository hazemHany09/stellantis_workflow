# Subagent Working File — Template

<!--
Path: runs/<run-id>/.harness/SubAgent/<agent-name>.md
Writer: subagent only, except the contract block which is pre-seeded by lead.
Structure:
  1. Contract block (lead-owned, subagent read-only)
  2. Working scratch (subagent appends)
  3. Result envelope (final JSON block — lead reads this after subagent returns)
-->

<!-- AUTOGENERATED BY LEAD — DO NOT EDIT -->
```json
{ ...see src/references/lead-to-subagent-contract.md for shape... }
```
<!-- END CONTRACT -->

# Subagent working file — <<agent-name>>

## Identity self-check

| Field      | Expected (from contract)    | Detected |
| :--------- | :-------------------------- | :------- |
| Agent name | `<<agent_name>>`            | `<<value read from this file's path stem>>` |
| Run ID     | `<<run_id>>`                | —        |
| Category   | `<<category>>`              | —        |

If any mismatch, abort and write `"status": "failed"` in the envelope without doing further work.

## Main loop (up to 3 iterations)

### Iteration 1

**Retrieval queries issued**

- `<<query>>` → top-k `<<n>>` → docs returned: `<<doc_ids>>`

**Docs read (chunks downloaded)**

- `<<doc_id>>` — chunks `<<chunk_ids>>`

**Per-parameter updates**

#### `<<parameter_id>>` — <<name_optimised>>

- Observed evidence snippets: `<<short quote or summary>>` → kind: `<<clear | vague>>`
- Traceability blocks added:
  1. `{ "source_name": "...", "source_link": "...", "source_justification": "...", "classification_justification": "...", "kind": "clear" }`
- Current verdict: Presence=`<<e>>`, Status=`<<e>>`, Classification=`<<e>>`, Rule=`<<Rule-n>>`

### Iteration 2
<!-- repeat structure, only for parameters still unresolved -->

### Iteration 3
<!-- repeat structure -->

## Fallback loop (up to 3 iterations)

Targeted hybrid retrieval for still-unresolved parameters. Uses the parameter's optimised name + description as the query context.

### Fallback iteration 1
<!-- same shape as main loop iteration -->

## Doc metadata staging (applied by lead, not by subagent)

List of per-doc metadata patches. The lead will batch these via `batch_update_doc_metadata`.

```json
[
  { "doc_id": "<<id>>", "metadata_patch": { "parameters_found": ["..."], "classification_High": ["..."] } }
]
```

## Warnings + advisories

- Warning (`parameter | category`): `<<msg>>`
- Advisory (`undefined_tier | out_of_list`): `<<msg>>`

## Notes

<<free-form markdown>>

---

<!-- RESULT ENVELOPE — LEAD WILL CONSUME THIS -->
```json
{ ...see src/references/subagent-to-lead-contract.md for shape... }
```
<!-- END RESULT ENVELOPE -->
